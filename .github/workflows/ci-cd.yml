name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  PYTHON_VERSION: '3.12'
  DJANGO_SETTINGS_MODULE: 'Nexus.settings'

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_USER: postgres
          POSTGRES_DB: nexo_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r nexo_dev/nexo/requirements.txt
        pip install pytest pytest-django coverage flake8 black

    - name: Create .env file for testing
      run: |
        cd nexo_dev/nexo
        cat > .env << EOF
        DEBUG=True
        SECRET_KEY=test-secret-key-for-ci
        DATABASE_URL=postgresql://postgres:postgres@localhost:5432/nexo_test
        ALLOWED_HOSTS=localhost,127.0.0.1
        EOF

    - name: Run linting with flake8
      run: |
        cd nexo_dev/nexo
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Check code formatting with Black
      run: |
        cd nexo_dev/nexo
        black --check --diff .

    - name: Run Django migrations
      run: |
        cd nexo_dev/nexo
        python manage.py migrate --noinput

    - name: Collect static files
      run: |
        cd nexo_dev/nexo
        python manage.py collectstatic --noinput

    - name: Run Django tests with coverage
      run: |
        cd nexo_dev/nexo
        coverage run --source='.' manage.py test
        coverage report
        coverage xml

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./nexo_dev/nexo/coverage.xml
        flags: unittests
        name: codecov-umbrella

  security:
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install security tools
      run: |
        pip install bandit safety

    - name: Run Bandit security linter
      run: |
        cd nexo_dev/nexo
        bandit -r . -f json -o bandit-report.json || true
        bandit -r . --severity-level medium

    - name: Check for known security vulnerabilities
      run: |
        cd nexo_dev/nexo
        safety check --json --output safety-report.json || true
        safety check

  build-and-deploy:
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r nexo_dev/nexo/requirements.txt

    - name: Create deployment package
      run: |
        cd nexo_dev/nexo
        # Criar .env de produção (sem valores sensíveis)
        cat > .env.production << EOF
        DEBUG=False
        SECRET_KEY=\${{ secrets.SECRET_KEY }}
        DATABASE_URL=\${{ secrets.DATABASE_URL }}
        ALLOWED_HOSTS=\${{ secrets.ALLOWED_HOSTS }}
        EOF
        
        # Coletar arquivos estáticos
        python manage.py collectstatic --noinput
        
        # Criar pacote de deployment
        tar -czf ../../nexo-deployment.tar.gz .

    - name: Deploy to staging
      if: github.ref == 'refs/heads/develop'
      run: |
        echo "Deploying to staging environment..."
        # Aqui você adicionaria o script de deploy para staging

    - name: Deploy to production
      if: github.ref == 'refs/heads/main'
      run: |
        echo "🚀 Iniciando deploy automático para produção..."
        
        # Configurar SSH
        mkdir -p ~/.ssh
        echo "${{ secrets.PRODUCTION_SSH_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan -H ${{ secrets.PRODUCTION_HOST }} >> ~/.ssh/known_hosts
        
        # Testar conexão SSH primeiro
        echo "🔍 Testando conexão SSH..."
        ssh -i ~/.ssh/id_rsa -o ConnectTimeout=10 ${{ secrets.PRODUCTION_USER }}@${{ secrets.PRODUCTION_HOST }} "echo 'SSH conectado com sucesso!'"
        
        # Executar deploy via SSH
        ssh -i ~/.ssh/id_rsa ${{ secrets.PRODUCTION_USER }}@${{ secrets.PRODUCTION_HOST }} << 'EOL'
          # Função para log com timestamp
          log() {
            echo "$(date '+%H:%M:%S') - $1"
          }
          
          # Parar em caso de erro mas com logs
          set -e
          trap 'log "❌ ERRO na linha $LINENO"' ERR
          
          log "🚀 Iniciando deploy automático..."
          
          # Verificar se diretório existe
          log "📁 Verificando diretório do repositório..."
          if [ ! -d "/home/eduardo/Documentos/nexo/nexo_dev_0001" ]; then
            log "❌ Diretório do repositório não encontrado!"
            exit 1
          fi
          
          cd /home/eduardo/Documentos/nexo/nexo_dev_0001
          log "✅ Navegado para: $(pwd)"
          
          # Verificar se é repositório git
          if [ ! -d ".git" ]; then
            log "❌ Não é um repositório Git!"
            exit 1
          fi
          
          log "📥 Fazendo backup da versão atual..."
          cp -r nexo_dev/nexo ../backup_$(date +%Y%m%d_%H%M%S) || log "⚠️ Backup falhou, continuando..."
          
          log "🔄 Atualizando código do GitHub..."
          git fetch origin || { log "❌ git fetch falhou"; exit 1; }
          git reset --hard origin/main || { log "❌ git reset falhou"; exit 1; }
          log "✅ Código atualizado com sucesso"
          
          # Verificar se diretório Django existe
          log "📁 Verificando diretório Django..."
          if [ ! -d "nexo_dev/nexo" ]; then
            log "❌ Diretório Django não encontrado!"
            exit 1
          fi
          
          cd nexo_dev/nexo
          log "✅ Navegado para Django: $(pwd)"
          
          # Verificar se manage.py existe
          if [ ! -f "manage.py" ]; then
            log "❌ manage.py não encontrado!"
            exit 1
          fi
          
          # Verificar se venv existe
          log "📦 Verificando ambiente virtual..."
          if [ ! -f "/home/eduardo/Documentos/nexo/venv/bin/activate" ]; then
            log "❌ Ambiente virtual não encontrado!"
            exit 1
          fi
          
          source /home/eduardo/Documentos/nexo/venv/bin/activate
          log "✅ Ambiente virtual ativado"
          
          # Verificar requirements.txt
          if [ ! -f "requirements.txt" ]; then
            log "❌ requirements.txt não encontrado!"
            exit 1
          fi
          
          log "⬇️ Instalando/atualizando dependências..."
          pip install -r requirements.txt || { log "❌ pip install falhou"; exit 1; }
          log "✅ Dependências instaladas"
          
          log "🗃️ Executando migrações do banco..."
          python manage.py migrate --noinput || { log "❌ migrate falhou"; exit 1; }
          log "✅ Migrações executadas"
          
          log "📋 Coletando arquivos estáticos..."
          python manage.py collectstatic --noinput || { log "❌ collectstatic falhou"; exit 1; }
          log "✅ Arquivos estáticos coletados"
          
          log "🔄 Reiniciando serviços..."
          sudo systemctl restart gunicorn_nexo || { log "❌ restart gunicorn falhou"; exit 1; }
          log "✅ Gunicorn reiniciado"
          
          log "⏱️ Aguardando serviço inicializar..."
          sleep 5
          
          log "✅ Verificando status do serviço..."
          if sudo systemctl is-active gunicorn_nexo > /dev/null; then
            log "✅ Gunicorn está ativo"
          else
            log "❌ Gunicorn não está ativo!"
            sudo systemctl status gunicorn_nexo --no-pager -l
            exit 1
          fi
          
          log "🏥 Verificando saúde da aplicação..."
          if curl -f http://localhost:8000 >/dev/null 2>&1; then
            log "✅ Aplicação respondendo em localhost:8000"
          elif curl -f http://127.0.0.1:8000 >/dev/null 2>&1; then
            log "✅ Aplicação respondendo em 127.0.0.1:8000"
          else
            log "⚠️ Aplicação não responde, mas pode estar inicializando..."
          fi
          
          log "📊 Status final dos serviços..."
          sudo systemctl status gunicorn_nexo --no-pager -l
          
          log "🎉 Deploy concluído com sucesso!"
        EOL

    - name: Upload deployment artifact
      uses: actions/upload-artifact@v4
      with:
        name: nexo-deployment
        path: nexo-deployment.tar.gz

  notify:
    runs-on: ubuntu-latest
    needs: [test, security, build-and-deploy]
    if: always()
    
    steps:
    - name: Notify on success
      if: needs.build-and-deploy.result == 'success'
      run: |
        echo "🎉 DEPLOY AUTOMÁTICO CONCLUÍDO COM SUCESSO!"
        echo "✅ Testes passaram"
        echo "✅ Verificações de segurança aprovadas"
        echo "✅ Deploy em produção realizado"
        echo "🌐 Aplicação atualizada e rodando com gunicorn_nexo"
        echo ""
        echo "📊 Pipeline completo executado em: $(date)"
        # Aqui você pode adicionar notificações (Slack, email, Discord, etc.)

    - name: Notify on failure
      if: needs.test.result == 'failure' || needs.security.result == 'failure' || needs.build-and-deploy.result == 'failure'
      run: |
        echo "❌ FALHA NO PIPELINE CI/CD!"
        echo ""
        if [ "${{ needs.test.result }}" = "failure" ]; then
          echo "❌ Testes falharam - verificar código"
        fi
        if [ "${{ needs.security.result }}" = "failure" ]; then
          echo "❌ Verificações de segurança falharam - vulnerabilidades detectadas"
        fi
        if [ "${{ needs.build-and-deploy.result }}" = "failure" ]; then
          echo "❌ Deploy falhou - produção não foi atualizada"
        fi
        echo ""
        echo "🔍 Verifique os logs para mais detalhes"
        # Aqui você pode adicionar notificações de falha urgentes 